## MODIFIED Requirements

### Requirement: 观测字段每轮无条件更新

事实层记录中反映「本轮观测到什么」的字段——最近一次事件时刻、最近同步时刻、事件计数、
受影响用户数、窗口天数——MUST 在每一轮观测到该 issue 时更新，无论抓取判定的结果如何。

该更新 MUST 由**确定性执行路径**完成，MUST NOT 依赖模型执行 prompt 来完成。

理由：观测字段所需的全部输入（计数、用户数、窗口、时刻）在模型返回的快照里已经具备，
回写它不产生任何额外的上游调用；而交给模型执行时实测 25 轮中仅 5 轮真正落盘，
且写出的时刻口径不稳定。**能确定性算出来的东西不交给模型**。

#### Scenario: 抓取被跳过但观测字段仍更新

- **WHEN** 某 issue 因计数未变或计数下降而跳过事件详情抓取
- **THEN** 其最近同步时刻被更新为本轮时刻
- **AND** 其事件计数与受影响用户数被更新为本轮观测值

#### Scenario: 衰减中的 issue 不被误判为停更

- **WHEN** 某 issue 连续多轮计数下降（修复生效、崩溃减少）
- **THEN** 每一轮的最近同步时刻都反映真实的观测时刻
- **AND** 台账与报告中该 issue 不呈现为「数据未同步」

#### Scenario: 模型未写入观测字段

- **WHEN** 模型路径执行完毕，且未写入任何观测字段
- **THEN** 调用方按快照内容回写全部观测字段
- **AND** 该 issue 的最近同步时刻是本轮时刻

#### Scenario: 观测字段回写失败

- **WHEN** 某 issue 的事实层文件无法被解析或写入
- **THEN** 该失败进入既有的落盘校验与隔离路径
- **AND** 不被静默忽略

## ADDED Requirements

### Requirement: 最近同步时刻的时间口径

最近同步时刻 MUST 是真实的 UTC 时刻，与产生它的执行环境的本地时区无关。
系统内 MUST 只有一个写入方产生该字段。

理由：实测出现过「本地时间加 `Z` 后缀」的写法，比真实时刻快 8 小时。
该偏差在跑批时刻与 UTC 同日时被日期粒度的判定掩盖，在跨日时则把**已正确刷新**的记录
判成未刷新——同一个缺陷在两种跑批时刻下表现相反。

#### Scenario: 跨 UTC 日界的跑批

- **WHEN** 跑批发生在本地日期与 UTC 日期不同的时刻
- **THEN** 写入的最近同步时刻仍是真实 UTC 时刻
- **AND** 新鲜度判定不因本地时区而改变结论

### Requirement: 新鲜度判定按时刻而非日期

事实层新鲜度断言 MUST 依据最近同步时刻与本轮时刻的差值，MUST NOT 仅比较日期。

#### Scenario: 时刻正确但跨日

- **WHEN** 某记录的最近同步时刻属于本轮，但其 UTC 日期与判定基准日期不同
- **THEN** 断言判定为新鲜

#### Scenario: 日期相同但时刻属于历史

- **WHEN** 某记录的最近同步时刻与本轮同日，但相差超过一轮跑批的时长
- **THEN** 断言判定为陈旧

### Requirement: 事实层内容覆盖率可见

事实层断言 MUST 输出「记录声称的事件数」与「实际存下的事件条数」的对比。
该输出 MUST NOT 改变断言的退出码。

理由：观测字段刷新与事件明细落盘是两件事，前者正常不蕴含后者正常——
实测存在「声称 55 条、实存 6 条」而断言全绿的状态。

#### Scenario: 观测字段新鲜但内容缺失

- **WHEN** 某记录的观测字段属于本轮，但其事件数组为空
- **THEN** 断言仍判定新鲜度通过
- **AND** 覆盖率对比在输出中可见
