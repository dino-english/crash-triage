## 1. 判定版本解析

- [x] 1.1 新增 `alert_ver()`：$1=plat → 输出告警判定版本。默认最新版；最新版 `adopt.sessions` < `SAMPLE_SESSION_MIN` 时回退为该端**会话量最大**的版本（`pick_top_sessions` 的第一个）
- [x] 1.2 解析 `IOS_ALERT_VER` / `AND_ALERT_VER` 与回退标志 `IOS_ALERT_FALLBACK` / `AND_ALERT_FALLBACK`
- [x] 1.3 ⚠️ **取值集中到一处**：6 个指标的告警取值统一改成从 `*_ALERT_VER` 取，**不要逐个调用点改参数**（design 风险表：6 个调用点容易漏改）

## 2. 摘要行标注

- [x] 2.1 回退生效时，摘要行追加说明：判定对象是哪个版本、为何不是最新版
- [x] 2.2 ⛔ **不得静默回退**（design D2）：换判定对象却不说，比漏报更难排查——数字对得上、版本对不上
- [x] 2.3 两端各自判断（一端回退、另一端不回退是常态）

## 3. 验证

- [x] 3.1 `bash bin/check-scripts.sh` 通过
- [x] 3.2 **当前数据即触发回退**（Android 1.5.4 会话数 1 < 30）：确认摘要行出现 `ANR 率 Android 1.5.3 0.71%` 且带回退说明
- [x] 3.3 确认 Android 1.5.4 的 `崩溃率 8.33%（3/36）` **不再触发红档**（误报消除）
- [x] 3.4 稳态等价性 ✅ 阈值调至 1 后不回退、判定对象回到最新版，且改动前的误报 `崩溃率 Android 1.5.4 8.11%` 重新出现——反证回退确实是消掉它的原因。⚠️ 首次测试用错了杠杆：`SAMPLE_SESSION_MIN` 原本是**硬编码**的，环境变量根本没被读；已改为可配
- [x] 3.5 表格呈现未受影响：版本列数、着色、小样本提示与改动前一致
- [x] 3.6 整跑一次确认无误告警

## 4. 收尾

- [x] 4.1 `CLAUDE.md` 的「阈值与告警」段更新：告警判定版本的回退规则与理由
- [x] 4.2 把 `crash-error-type-coverage` 的 findings F1 标为已解决，指向本 change
