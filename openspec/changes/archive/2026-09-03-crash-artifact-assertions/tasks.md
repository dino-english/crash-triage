## 1. 实施（2026-09-02 已落地，本 change 为追认）

- [x] 1.1 `bin/test/assert-artifacts.sh`：id 单元格链接 / href 形状 / app id 与平台一致
- [x] 1.2 卡片断言：表格单元格无链接、列名不撞 CardKit 平台变体键
- [x] 1.3 占位符检查默认关闭（未投递产物本就带占位符），`CHECK_PLACEHOLDERS=1` 开
- [x] 1.4 挂进 `crash-daily.sh` 与 `crash-weekly.sh` 的投递之前，⛔ 放在 `if` 条件位
- [x] 1.5 ⚠️ 只告警不中止

## 2. 验收

- [x] 2.1 **红**：今早生产那份坏产物 → 精确点出 Android 四段的坏链接（2 通过 1 失败）
- [x] 2.2 **绿**：修复后那份 → 4 通过 0 失败
- [x] 2.3 `set -e` 下断言失败不中止、不触发 ERR trap（实测分支正确触发并继续执行）
- [x] 2.4 `bash bin/check-scripts.sh` 九项通过
- [x] 2.5 **真实定时跑批验证**：2026-09-03 08:30 的 L1 跑批日志含「产物自检：4 通过，0 失败」

## 3. 文档

- [x] 3.1 登记 F44（只验改动维度等于没验产物；含写断言时踩的两个坑）
- [x] 3.2 归档本 change

## 实施记录

- ⚠️ **写这个脚本本身踩了两个坑**，都写进 F44：
  ① `echo "…（$PUB）…"` 全角右括号被 bash 并进变量名（正是本仓库常驻约束那条）；
  ② 判据改了三版——初版「有 issue 行就该有链接」把没有 id 的维度表全判失败（假阳性）；
  二版「id 单元格必须是链接」漏掉 `<a href="裸32位id">`（点了 404 但有 `<a>`，假阴性）；
  三版「只看每行首列」才排除掉「修复提交」列里同样是 8 位 hex 的 commit。
- ⚠️ 2026-09-03 另修一处：`skip()` 把「产物不存在」硬拼进函数，导致
  「（未投递产物本就带占位符）（产物不存在，跳过）」自相矛盾——原因改为可传参（`57af6fa`）。
