## 1. 先实测渲染（决定后续所有版面细节）

- [x] 1.1 ✅ 已真发（2026-08-22 15:54，私聊，自测闸门跳过归档/索引页/台账）。**首次失败**：`ios`/`android` 是 CardKit v2 保留列名（平台变体键），报 `invalid name:harmony`——`jq empty` 与结构检查都发现不了（findings F4）。改列名为 `col_ios`/`col_and` 后发送成功
- [x] 1.2 重点看三处：表头 `1.5.4(82)→1.5.3(287)` 是否截断；单元格 `20 次 / 10 人 ↓34` 是否换行；6 行是否触发分页
- [x] 1.3 在**手机端**看一次，不只是桌面端——「别让用户滑」的判据在手机上
- [x] 1.4 若表头截断 → 退化为只写版本号、会话数移回独立行；若单元格换行严重 → 去掉「/ N 人」或拆行。**把实测结论记进 design**，不要留在对话里

## 2. 行定义拆分（最容易漏的一处）

- [x] 2.1 `ROW_DEFS` 拆成 `ROW_DEFS_CARD`（6 行）与 `ROW_DEFS_DOC`（全量）
- [x] 2.2 三个调用点各自指定：`build_table()` 用 CARD、`md_table()` 用 CARD、`xml_table()` 用 DOC
- [x] 2.3 ⚠️ **验收确认文档仍是全量** —— **首次实施时真的漏了**（漏的是 markdown 文档那条路径，见 findings F1），grep `会话数 1d` 得 0 次才发现。已补 `md_table_doc()`。原文：验收必须逐项确认文档仍是全量——直接改 `ROW_DEFS` 会把文档一起砍到 6 行，而文档的完备性正是卡片敢砍的前提（design D3）

## 3. 卡片重构

- [x] 3.1 `build_table()` 重写：列语义从「版本」改为「平台」，输出单表 `指标 | iOS | Android`
- [x] 3.2 表头拼装：`iOS 1.5.4(82)→1.5.3(287)`，括号内为会话数（design D2）
- [x] 3.3 单元格承载「值 + 对比」，`delta_cell()` 原样复用（箭头跟数值方向、颜色跟好坏，规则不变）；**不设独立对比列**（design D4）
- [x] 3.4 六行：Crash-free 会话率 / 崩溃（事件+人数）/ ANR / 非致命 / 启动 P95 / 慢帧最差页（**带样本量**）
- [x] 3.5 iOS 的 ANR 格渲染为「— 无此概念」，**不是 `—`、不是 `0`**（design D5）
- [x] 3.6 新增「🎯 影响集中」markdown 行：top 3 机型 + 各自受影响人数（design D6，数据取自 `crash-dimensions.sql`）
- [x] 3.7 `md_table()` 回退视图同步为单表形态
- [x] 3.8 ⚠️ **主力版本补列的退化处理**：单表形态下第三个版本无处安放，退化为表头标注 + 文档展开。**不能默默丢掉**（design 风险表）
- [x] 3.9 摘要行判定逻辑**一行不动**（红黄档、只由最新版触发、无值不告警）

## 4. 移出卡片的项确认仍在文档

- [x] 4.1 逐项确认文档保留：启动 P50 · 冻结率 · 接口错误率 · 会话数独立行 · DoD/WoW 表 · 版本放量表
- [x] 4.2 口径注保留「只统计最新 2 个版本」——单表化后版本不再是独立列，不写会被误以为口径变了

## 5. 验证

- [x] 5.1 `bash bin/check-scripts.sh` 通过
- [x] 5.2 `CRASH_REPORT_DRY_RUN=1 bash bin/crash-daily.sh` 看卡片预览
- [x] 5.3 完整链路 ✅ 文档版本对照恢复 13 行 × 版本列（含主力第三列），卡片 3 列 × 6 行；`启动 P50` / `冻结率` / `接口错误率` / `会话数 1d` / `受影响安装` 逐项确认仍在文档
- [x] 5.4 已发送到私聊（16:44 那版含全部修正）；**手机端目视确认待你反馈**——重点看表头 `1.5.4(28/14)→1.5.3(1243/877)` 是否截断
- [x] 5.5 所有数值与文档对得上（本 change 是纯版面重排，数字不应有任何变化）

## 6. 收尾

- [x] 6.1 `CLAUDE.md` 的「卡片与对比列」一节重写：形态从「每端一表、列为版本」改为「单表、列为平台」，说明版本进表头、对比进格内
- [x] 6.2 记录规格漂移的修正：`crash-perf-daily-card-v2` 的 spec 自 2026-08-18 起与实现不一致（`crash-perf-latest-2-versions` 改了卡片但没更新卡片 spec），本 change 一并修正
