# 实施中发现

## F1 定时炸弹尚未引爆，赶在前面修了

实测全部 7 条 bq 事实层记录，**存档计数与当前 7 天窗口计数逐条一致**——尚无衰减。
最早的事件在 2026-08-17，**08-24 起开始出窗**，届时计数下降、`latest_event` 与 `last_synced` 会开始冻结。

修复后立刻见效：`a34175e5` 的 `latest_event` 从 **08-17 03:02 前进到 08-21 12:23**。
旧实现下这条会因为「计数没变」落进 hit 分支、永远停在 08-17。

## F2 ⚠️ 我引入过一个回归：更新时覆盖了 `.source`

首版实现在更新分支里写了 `.source="bigquery"`，把**模型路径记录的来源标签抹掉了**——
7 条原本无 `source` 键（模型路径）的记录被改标成 `bigquery`。

`source` 的作用是区分「只有聚合事实」与「有完整事件数组」。events 数组其实还在
（`3e827b74` 仍有 65 条），但标签说没有——**信息被销毁的是元数据不是数据，更隐蔽**。

**已修**：更新分支不碰 `.source`，该字段由创建者写死。被覆盖的 7 条已从跑批前快照还原。

**教训**：「无条件刷新观测字段」的范围要精确到字段列表，不能顺手把整条记录重写。
`source` 不是观测字段，是**溯源元数据**。

## F3 `window_days` 有 5 条「缺失」是正确的

19 条记录里 14 条写入了 `window_days`，5 条缺失。那 5 条是本轮快照里**没有出现的 issue**
（已完全出窗），本轮压根没观测到它们——不写才对。产物断言只对快照里出现的 issue 断言，与此一致。

## F4 prompt 去重后的验证方式

两处 prompt 已合并为单一变量 `FACT_CACHE_POLICY`（1 份定义 / 2 处引用，已 grep 确认）。
但**这只防住「改漏一份」，防不住「模型没照做」**。

故新增 `bin/test/assert-fact-cache.sh` —— 对**落盘产物**断言（`last_synced` 是本轮 / `window_days` 已写入 /
`latest_event` 未倒退）。实测 14 个 issue 全部通过。

## ⛔ 未完成

- **4.6 模型路径验证**：需要跑一轮不带 `CRASH_REPORT_SKIP_ANALYSIS` 的 full 模式（会调用 `claude -p` 与 MCP）。
  本轮全部验证都走 `SKIP_ANALYSIS=1`，**模型路径的 prompt 改动未经真实执行验证**。
  产物断言脚本已就绪，跑完 full 模式后直接执行即可。
- **5.1 同步 `crash-perf-functional-core` 的用例**：那个 change 尚未实施，其 task 3.5 要求用例
  「钉住当前行为并标注已知缺陷」——现在缺陷已修，该任务描述需相应改写（已在其 tasks.md 中更新）。
