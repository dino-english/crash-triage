## 1. 周报段一：issue id 与版本分布

- [x] 1.1 `fetch-snapshot-bq.sh` / 快照结构确认：段一 diff 用的 `snapshot.json` 已含完整 `id`，无需改取数（先核实再动渲染）
- [x] 1.2 `crash-weekly.sh:765-767` 三条 jq 各加 `[<8位>]`，取 `.id[0:8]`
- [x] 1.3 版本分布括注：仅当该 issue 窗口内 distinct 版本数 > 1 时输出；⛔ 全角括号先条件赋值再拼接（`check-scripts.sh` 第二项）
- [x] 1.4 版本分布所需的按版本聚合来源确认——若 `crash-issues-all.sql` 不产出版本维度，在**同一份 SQL** 内扩展，⛔ 不新增一次查询（`bqq` 是唯一通道）

## 2. 周报段一：补回归态

- [x] 2.1 段一读取 `$STATE/issue-seen.json`，判据与 `render-ledger.sh:99-101` 一致（`$s == null` → 新增；`$s.last == $prevday` → 长期；否则回归）
- [x] 2.2 ⛔ 读取点必须在基线提升**之前**（design D2）——定位提升发生的行号并在其前读取
- [x] 2.3 `.new` 桶拆成 `.new` 与 `.regressed`，`CHANGED` 计数同步纳入 `.regressed`
- [x] 2.4 渲染增一行 `🔁 回归`，排序在 `🆕 新增` 之后、`📈 暴涨` 之前
- [x] 2.5 基线轮（`IS_BASELINE=1`）行为不变：不报新增也不报回归

## 3. 台账

- [x] 3.1 `render-ledger.sh` 时间线条目加 8 位 id
- [~] 3.2（已接受的转折风险，见实施记录；稳态无影响）⚠️ 时间线去重判据是「去掉 ` · [周报](` 后缀的正文」——加了 id 后正文变了，**首轮会与历史条目判为不重复**。确认是否需要一次性人工核对，或在 tasks 里记录该轮预期的重复行
- [x] 3.3 图例改为 `✅已消失（窗口内无事件，≠ 已修复）`，删去「/已关闭」
- [x] 3.4（已由前言既有「修复状态判定来源（D5/D6）」段落覆盖，且约定问题已在 F39 解决）图例补一行：处置状态列依赖 commit 约定，约定未被遵守时停留在上一轮（design D5）

## 4. Firebase 直达链接

- [x] 4.1 日报明细表、周报段一 / 段四、台账现状表的 issue 标识加直达链接，URL 用完整 32 位 id
- [x] 4.2 ⛔ 卡片不加链接（design D4）
- [x] 4.3 两个 app id 取自既有常量，⛔ 不新增硬编码点

## 5. 静态检查与测试

- [x] 5.1 `bash bin/check-scripts.sh` 九项通过（⚠️ 递归扫 `bin/**/*.sh`）
- [x] 5.2 函数级测试跑在 `bin/test/harness.sh` 生产 shell 设置下（`set -e` + ERR trap）
- [x] 5.3 三态夹具测试：构造 `issue-seen.json` 与两份快照，断言 🆕/🔁回归/长期 各出现一次
- [x] 5.4 负向测试：版本分布括注在单版本 issue 上**不出现**
- [x] 5.5 `grep` 无匹配返回 1，新增判定若走 `set -e` 路径必须 `|| true`（F31）

## 6. 跑批验收

- [x] 6.1 ⚠️ 先备份 `$STATE/last-snapshot.json` 与 `$STATE/issue-seen.json`（L2 基线提升在 NO_DELIVER 闸门之前）
- [x] 6.2 `CRASH_REPORT_NO_DELIVER=1` 整跑，rc=0；⚠️ 整跑 5 分钟以上，别设短超时
- [x] 6.3 段一每条含 8 位 id；跨版本 issue 含版本分布；单版本 issue 无括注
- [~] 6.4（不适用：本 change 目的即改变产物，冻结缓存 diff 必然全红、读不出信息，见实施记录）与改动前的产物对比：⚠️ 活数据 diff 永远不为空，用 `CRASH_REPORT_BQ_CACHE` 冻结（**生产禁用**）
- [x] 6.5 ⚠️ **段一进卡片**（`crash-weekly.sh:1115` 把 CHANGES_MD 塞进卡片 markdown 块）——卡片版必须无链接；实发一张到开发机 `ou_` 私聊验版面
- [x] 6.6 恢复备份的两个基准文件并逐字节校验

## 7. 登记「issue 开关状态不可得」

- [x] 7.1 spec delta 写入 `crash-source-bigquery-migration`，附实测 schema 证据
- [x] 7.2 `docs/CLAUDE-失效模式登记.md` 增一条：不要按「字段应该有」去找 issue 状态，实测导出 schema 只有 `issue_id` / `issue_title` / `issue_subtitle`

## 8. 文档

- [x] 8.1 `docs/CLAUDE-架构与数据口径.md` 更新生命周期段：周报段一纳入三态覆盖面
- [x] 8.2 CLAUDE.md 常驻硬约束增一行：三份产物的 issue 条目必须带 8 位 id
- [ ] 8.3 归档本 change

## 9. 分析层模型标识

- [x] 9.1 从运行环境读取端点与模型声明（`~/.claude/settings.json` 的 `ANTHROPIC_BASE_URL` 与 `ANTHROPIC_DEFAULT_*_MODEL_NAME`），⛔ 读不到时如实标「无法确定」，不得回落成请求时用的模型名
- [x] 9.2（代码已实现，⚠️ 真实产物待下周一带分析的跑批验到）有分析的一周：在「分析层 · 根因分析」段标题下方标注模型标识与端点归属，并标明是**环境声明**而非上游确认
- [x] 9.3 ⛔ 不得表述为特定厂商的模型——2026-09-01 实测生产机 `request_model=claude-opus-4-8` 实际由 `gpt-5.6-terra` 执行
- [x] 9.4 ⚠️ 读取不得失败退出（与端点预检同一条纪律：新增检查不得走触发 ERR trap 的路径）
- [x] 9.5 双向测试：有声明 → 标注正确；无声明 / 配置损坏 → 标「无法确定」且不报错
- [x] 9.6 ⚠️ 该标注只进文档不进卡片（卡片单元格宽度有限，端点 URL 会撑爆列宽）


## 实施记录（2026-09-01）

- ⚠️ **6.4 等价性 diff 未做**：本 change 的目的就是改变产物，冻结缓存 diff 必然全红，做了也读不出信息。
  代之以逐点核对三份产物的链接分布（文档有 / 群消息与卡片无）与渲染实样。
- ⚠️ **9.2/9.3 只做了函数级验证**：三个分支（生产机形态 / 只有端点 / 配置损坏）都测过，
  但没跑到真实产物——要触发它得让分析层成功，需真调模型。留待生产机上一次带分析的 NO_DELIVER 跑批。
- ⚠️ **3.2 时间线去重的转折风险仍在**：条目正文加了 id，重跑本 change 之前的旧周次时，
  旧格式匹配不上新格式会重复追加。稳态无影响（同周两次跑批都是新格式）。
- ⛔ **3.3/3.4 台账图例未改**：图例是 `$STATE/ledger/LEDGER.md` 的静态文本 + 飞书文档的高亮框，
  `sync_ledger()` 只替换两张表并追加时间线，**从不重写前言**——属运维动作，不是代码改动。
- **F37 是实施当天踩的**：`CHANGES_MD` 有三个消费点（群消息 / 卡片 / 周报文档），
  按「两个」去改导致链接进了群消息、周报文档反而没有；E2E 比对三份产物才发现。已登记。