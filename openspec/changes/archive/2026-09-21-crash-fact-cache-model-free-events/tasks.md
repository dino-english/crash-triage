## 0. 决策

- [x] 0.1 YAML 方案 **选 A：装 PyYAML**（2026-09-18 拍板）——
      ⚠️ 本仓库首次引入 python 三方依赖，装机/换机流程随之多一个失败点，
      `setup.sh` 与 `install.sh` 都要覆盖到，⛔ 缺依赖 MUST 在装机时就报错而非跑批时

## 1. 基线取证（已完成，2026-09-18）

- [x] 1.1 证明大结果不下发给模型
      → 验：轨迹里出现 `<persisted-output> Output too large`
      **实测**：55.1KB / 77.7KB / 312.9KB 三次，模型只得到 2KB 预览 + 磁盘路径
- [x] 1.2 证明「补不上」随待抓量单调恶化，⛔ 排除「随机」
      → 验：空 STATE 制造 6 个全量待抓，看写入数
      **实测**：写入 **0 个**；待抓 1 时写入 1（断言 6/6 过）
- [x] 1.3 证明放开权限救不了
      → 验：查生产机 python 有无 PyYAML
      **实测**：`/usr/bin/python3` 与 `/opt/homebrew/bin/python3` **都没有**
- [x] 1.4 证明裸调 MCP 可行
      → 验：不经模型取回某 issue 的事件
      **实测**：`227fc097` 取回成功，⚠️ 参数须 camelCase 且 `issueId` 嵌在 `filter` 内

## 2. 确定性抓取路径

- [x] 2.1 新增裸调 MCP 的取数脚本（stdio JSON-RPC 三步握手）→ `bin/fetch-events-mcp.py`
      **实测（2026-09-18 生产）**：6 个全量待抓 → **6 个全落盘、72 条真事件、0 失败**。
      ⛔ 对照：同样 6 个待抓，模型路径写入 **0 个**（1.2 的实测）。
- [x] 2.2 ⚠️ 显式传 `intervalStartTime` / `intervalEndTime`（脚本按 `--days` 自算）
      **实测**：`--days 89` 下 `62f88f39` 取回 50 条（pageSize 上限）、`85c581ed` 12 条，
      均早于 7 天窗；⚠️ 参数须 camelCase 且 `issueId` 嵌在 `filter` 内。
- [x] 2.3 429 退避两轮（30s/60s）+ 自带看门狗 `--timeout`（默认 600s）——
      ⚠️ 本脚本不 source `bin/lib.sh`（副作用太大）故不用 `run_with_timeout`。
      **已实测**：`bin/test/fn-fetch-events-mcp.sh` 用假 MCP server 打开了这条路径——
      两轮内恢复 → 事件照抓且不算失败；超过两轮 → 非零退出、不落盘、原因说人话。
      ⚠️ 退避秒数用 `CRASH_REPORT_MCP_BACKOFF` 覆盖**只为夹具**（真等 90 秒没人会跑它）。

- [x] 2.4 ⛔ **夹具抓到的真 bug**：MCP 的 YAML 用字面块标量（`eventId: |`），
      解析出来带引号和尾换行 `"'2264847276362233513'\n"`，而事实层既有记录是干净的
      `"2242955368866323656"`。两种形状混在一起**去重整个失效**——实测重抓同一 issue
      2 条变 3 条，每轮再复制一遍。已加 `clean_scalars()`：只还原顶层字符串标量，
      ⚠️ `device` / `threads` 这类多行文本块原样保留。
      **生产已订正**：那条用坏形状写入的记录已移出留档并重抓，
      全仓复查 **28 文件 194 事件 · 占位 0 · 形状异常 0**。

## 3. 接线与降级

- [x] 3.1 `fetch-snapshot.sh` 的事实层事件改走 2.1；prompt 的 `FACT_CACHE_POLICY`
      改成「**你完全不要碰它**」。⚠️ 仍留一句而不是整段删除——2026-09-18 实测
      不说「别碰」它会自己去碰。⛔ `requires approval` 归零待生产跑批后核。
- [x] 3.2 ⛔ 已删掉模型自报统计；改由确定性脚本输出「跳过/全量/增量/失败/新增事件」，
      成败以 `assert-fact-cache.sh` 为准。夹具断言 `失败 F 个` 不得再出现在 prompt。
- [x] 3.3 新增「模型被权限闸拦下 N 次」提示。⚠️ 只提示不判失败——被拒本身不是故障
      （09-13 那 4 次是版本探测）。⛔ 按 F21 写：`grep -c` 无匹配返回 1 且不输出，
      `|| echo 0` 会得到两个 0，只能先接空串再补 0。
      **双向测试**：注入 2 条被拒 → 报「拦下 2 次」且 rc=0；正常轮次不误报。

## 3b. 断言看内容（⛔ 2026-09-18 新增，实测毒记录后追加）

- [x] 3b.1 `assert-fact-cache.sh` 增「事件条目含真实数据」断言
      → 验：⚠️ **双向测试**——注入占位记录当场变红 + 全量 27 个真记录不误报
      **实测（2026-09-18，生产 27 个真记录取到本地跑）**：
      占位记录 → `❌ 227fc097 事实层有占位条目（5 条里只有 0 条是真事件）`；
      27 个真记录 **零误报**，「已存累积事件」仍是 189 条（与旧口径一致）；
      ⛔ **同一份输入喂给改动前的旧断言，命中 0 次**——毒记录当年正是这样通过的
- [x] 3b.2 `cache_verdict` 的「已存条数」MUST NOT 把占位条目计入
      → 验：占位记录 → 进欠账清单（⛔ 不是跳过）；真记录不被误判
      **做法**：判据收进 `fc_real_events()`（`bin/lib/factcache.sh`），
      ⛔ 两条取数路径（`fetch-snapshot.sh` / `fetch-snapshot-bq.sh`）与断言**同源**，
      不许各写一份 jq——那正是本文件顶部记的 FACT_CACHE_POLICY 漂移。
      **夹具**：`fn-fetch-snapshot-shell.sh` 新增独立用例（占位必进清单 + 真事件不误判），
      ⚠️ 顺带订正了 3 处旧夹具——它们用 `{"e":1}` 代表「一条真事件」，
      而真实事件 189/189 都带 `eventId`，⛔ 夹具形状不真实，改动一上来就把它照出来了
- [x] 3b.3 存量排查：全仓事实层扫一遍占位记录
      **2026-09-18**：28 个文件中 1 个中毒（`227fc097`，5/5 占位），
      已移出 `issues/` 留档 `backup/poisoned-227fc097-20260918.json`。
      **2026-09-21 复扫（部署后连续四轮跑批之后）**：32 文件 247 事件，
      ⛔ **占位 0 · 形状异常 0**——新代码没有再产生任何一条。

## 4. 验收

- [x] 4.1 空目录复跑：6 个全量待抓**全部落盘**，72 条事件逐个核过 `总条数 == 真事件数`，
      抽样字段真实（eventId / eventTime / device / blameFrame 带文件行号符号）。
      **生产事实层占位复查：0 / 28**。
- [x] 4.1b ⚠️ **开发机跑不了 `fn-fetch-events-mcp.sh`**（系统 python 是 PEP 668 托管的）。
      ⛔ 真正危险的不是「跑不了」，是它**原本 exit 0 静默跳过**——混在一片 ✅ 里
      看起来像通过了，与 F51「一个不会红的检查项」和盲区⑥ 同源。
      **已做**：约定夹具用**退出码 77** 自述「缺依赖、跳过」；`check-scripts.sh` 单独计数，
      在「✅ 全部通过」**之前**打出「⚠️ N 个夹具被跳过……⛔ 跳过不是通过」。
      绕法写进夹具头部注释（venv 两行）。
      **双向测试**：有 PyYAML → 夹具真跑、无跳过行；塞一个故意失败的临时夹具 →
      退出码 1 且无「全部通过」，⛔ 证明 77 的特判没吞掉真失败。
      ⚠️ 「要不要把 venv 固化成仓库约定」仍**待人定**——本条只保证洞是可见的，没有填上它。

- [x] 4.1c **已部署**（2026-09-18，`97068e1` → `a944d85`）。生产核验：
      HEAD 对得上 · 工作区干净 · `/bin/bash bin/check-scripts.sh` 全绿（bash 3.2.57）·
      两个 cron 在位（daily 09-19 08:30 / weekly 09-21 05:30）·
      ⚠️ `update.sh` 的 `❌ setup.sh 失败` 与「无 hermes cron 任务」是**已知假故障**
      （非交互 ssh 无 brew PATH）——本次逐条证伪过：`path.env` mtime 仍是 08-20 未被动，
      带真实 PATH 实跑 `setup.sh` rc=0 且打印 `py-yaml 已就绪`，写出的 path.env 内容一致。
      ⛔ 不能因为「记着是假故障」就跳过——本次刚好改过 setup.sh，必须实测才敢这么说。
      **新夹具在生产真的运行了**（那里有 PyYAML），22 条 0 失败。

- [x] 4.2 部署后连续四轮核验（2026-09-19 / 20 / 21 的 L1 + 09-21 的 L2），**全部通过**：

      | 轮次 | 确定性取数 | 被权限闸拦下 | 产物断言 |
      | --- | --- | --- | --- |
      | 09-19 L1（首轮） | 全量 1 · 新增 **2 条** · 失败 0 | **0** | 0 ❌ · 待补 0 |
      | 09-20 L1 | 跳过 5 · 失败 0 | 1 次（见下） | 0 ❌ · 待补 0 |
      | 09-21 L1 | 跳过 5 · 失败 0 | **0** | 0 ❌ · 待补 0 |
      | 09-21 L2 | 增量 3 · 新增 **51 条** · 失败 0 | 0 | 0 ❌ |

      **事实层 2026-09-18 → 09-21：28 文件 194 事件 → 32 文件 247 事件**，
      新增 53 条全部由确定性路径抓回；⛔ **占位 0 · 形状异常 0**。
      L2 四层 + 台账全过：`run.end ok:true` · FATAL/NON_FATAL 两表已换 ·
      变更时间线追加 20 行且历史保留。

      ⚠️ **09-20 那 1 次被拒值得记**：被拒的是
      `python3 -c 'from pathlib import Path; Path(".../runs/2026-09-20/L1/…")'`——
      写的是**本轮输出目录**不是 `issues/`，多半在用 python 写 `snapshot.json`。
      ⇒ prompt 的「别碰事实层」生效了（事实层零被碰），但**模型伸手抓解释器的习惯还在**，
      只是换了目标。⛔ 那个目标的 payload 小、`Write` 够用，所以没有损失——
      ⚠️ 但这说明 F52 的根因（模型偏好脚本执行）没有消失，只是被挪出了危险区。
      新增的「被权限闸拦下 N 次」提示正是为此保留：它在 09-20 如实报了出来。

      ⚠️ 判据按「这轮到底补上几条」核的，不是「代码路径对不对」——
      53 条是数出来的，不是推出来的。

## 5. 禁读回归与修复（2026-09-21）

- [x] 5.1 ⛔ **回归**：prompt 的「不许写」被写成「不要读它、不要写它」，
      L2 周报证据等级 ✅钻取确认 **4 处 → 0 处**、⚠️聚合推断 **0 → 6 处**。
      模型原话：「未读取…事实层缓存，因此均为 ⚠️聚合推断」。⇒ 封锁能力要分清读与写。
- [x] 5.2 改成「⛔ 只读不写」并在夹具钉死（必须出现「要读」、必须不出现「不要读它」）。
      ⛔ 顺带一个险些上生产的坑（F54）：新 prompt 里用了**未转义反引号**，
      双引号串里那是命令替换——`bash -n` 照样通过，夹具当场从 0 失败变 30 失败才拦下。
- [x] 5.3 **修复已验，⛔ 没有等下周一的跑批**：`fetch-snapshot.sh full` 单跑
      （STATE 指向副本、事实层拷 32 个真文件、⛔ 它自己不投递、不提升基线）：

      | 版本 | ✅钻取确认 | ⚠️聚合推断 |
      | --- | --- | --- |
      | 09-14 改动前 | 4 处 | 0 |
      | 09-21 生产（带禁读 bug） | **0 处** | 6 处 |
      | 09-21 修复后单跑 | **5 处** | 1 处 |

      ⚠️ 比改动前**还好**（5 > 4）——事实层多出来的事件真的被用上了。
      五处**全部注明实样本**（`1/1`、`1/10`、`至少 1/3`），不是空贴标签；
      其中一处还区分了「证据等级=钻取确认、根因仍为聚合推断」，分寸是对的。
      ⚠️ 该轮仍报「被权限闸拦下 2 次」——与 09-20 同因（模型改用 python 写
      `snapshot.json`，不碰事实层），⛔ 习惯未消失只是被挪出危险区。

- [x] 5.4 ⛔ **没有等 09-28**（2026-09-21 当天做完）。整条 L2 端到端跑了一次
      （`CRASH_REPORT_NO_DELIVER=1 crash-weekly.sh`，跑前按 [[l2-dryrun-not-a-gate]]
      备份四个状态文件 + `ledger/` + `issues/`）：

      | 版本 | ✅钻取确认 | ⚠️聚合推断 |
      | --- | --- | --- |
      | 09-14 改动前 | 4 处 | 0 |
      | 09-21 生产（带禁读 bug） | 0 处 | 6 处 |
      | 修复后单跑分析步骤 | 5 处 | 1 处 |
      | **修复后整条 L2 端到端** | **6 处** | **0 处** |

      本轮 0 次权限拦截 · 确定性取数 0 失败 · 产物自检 2 通过 0 失败。
      ⚠️ 实测证实那条约束是真的：`last-snapshot.json` 跑完 7596 → **7664 字节**，
      不备份就会污染下周一的 diff。跑完四个文件 + `ledger/` 已恢复并逐个 `cmp` 校验一致，
      `report-index.jsonl` 未追加假归档，飞书零投递。
      ⚠️ 真副作用一处：`reports/2026-09-21-weekly.md` 被覆盖成修复后的版本。

- [x] 5.5 spec 并入正典时订正了一处**我自己埋的矛盾**：delta 原写
      「产物断言失败 → 跑批 MUST 非零退出」，而正典既有条文明写
      ⛔「MUST NOT 使跑批失败——为它中止跑批会把次要降级升级成主要故障」。
      代码实现跟的是正典（只告警不失败），是 delta 文本写错了，已订正后再并入。
      ⚠️ 归档不是挪目录——delta 与正典冲突时，**先查实现跟的是哪一边**。
