## 1. 完整日窗口（A）

- [x] 1.1 `bin/lib/core/format.sh` 新增 `last_complete_day()`：入参 `DATA_UNTIL`，出参 `YYYY-MM-DD`（`DATE(DATA_UNTIL) - 1`）。`DATA_UNTIL` 为 `—` 时返回空，调用方走既有缺数三态第 2 态
- [x] 1.2 完整日标注 ⚠️ **改为新增 `win_days()` 而不是给 `win_full()`/`win_compact()` 加模式**：那两个函数产出的是「双时区时刻串」（`08-15 17:22 → 08-16 14:59 (+08) · …`），完整日窗口没有时刻也没有时区歧义，塞进同一个函数只会让两条分支共用一个名字却毫无共用逻辑。⛔ 旧两函数**一字未动**，崩溃段与放量段照原样走（Non-goal 保持）
- [x] 1.3 `crash-daily.sh` 性能段 SQL 改为 `DATE(event_timestamp) BETWEEN {{LCD_START}} AND {{LCD_END}}`，**不再用 `TIMESTAMP_SUB(CURRENT_TIMESTAMP(), ...)`**。占位符经 `q_render` 收口，漏传当场失败
- [x] 1.4 `crash-weekly.sh` 性能段同步改为 `[LCD-6, LCD]`（实测渲染出 `2026-08-19 ~ 2026-08-25`，三份 SQL 过 `bq --dry_run`）。⚠️ L2 的 `PERF_LCD` **必须由性能表的 MAX 算**——`DATA_UNTIL` 在 L2 取的是 sessions 活表，恒为「刚才」，拿它算 LCD 会得到今天
- [x] 1.5 DoD 改 `LCD` vs `LCD-1`（删除 `perf_day_offset()`，全平台共用一个 LCD）；WoW 新增 `hist_val_perfday()` 按**记录里的 `perf_day`** 取 `LCD-7`，⛔ 不再按跑批日 `.day` 取——旧写法两边相隔几天完全不确定（实测 2026-08-22 那轮 iOS 1.5.4 记 08-17、1.5.3 记 08-18，同一次跑批差 1 天）。`perf_day`/`perf_prev_day` 下游只有渲染，无硬编码依赖
- [x] 1.6 标注文案升级为「性能数据 X ~ Y（完整日）· 滞后 N 天 · Z 起未合并」，卡片与文档同步。**`DATA_UNTIL` 仍要显示**（design D2）
- [x] 1.7 未新增查询，且**净减 4 条**：审计实测 2026-08-24 旧代码 76 条 → 本轮 72 条，减掉的正是 4 个版本各一次的 `perf_day_offset`
- [x] 1.8 验证（2026-08-27 16:19 dry-run，check-scripts 九项通过、退出码 0）：

  ⚠️ **本条原定的两个数字对不上当天数据，已按实测重写**。原文说「P95 1038 → 927、半天值 1044」，
  但 2026-08-27 直连 bq 复核：08-25 是**完整日**（24 小时 192 样本 P95 1044），残日是 08-26（7 小时 29 样本 P95 548）。
  提案 ① 表里标成「仅尾部半天」的那一行（192 / 281 / 1044），实际是完整日 08-25 的全天值——**标签错位**。
  故 ① 的「尾部 7 小时贡献 49% 样本、抬高 P95 12%」不成立于当天数据；②（DoD 拿残日比完整天）成立且更严重。

  实测（iOS 1.5.4 `_app_start`）：

  | 口径 | 样本 | P50 | P95 |
  |---|---|---|---|
  | 旧 `run−3d` 滚动窗 | 350 | 278ms | **1032ms** |
  | 新 `[08-23, 08-25]` 完整日 | 477 | 281ms | **980ms** |
  | 残日 08-26（7 小时） | 29 | 278ms | 548ms |

  报告实际渲染出 **980ms**，与直连 bq 一致；DoD 由「残日 08-26 vs 完整日 08-25」变为「08-25 vs 08-24」
  （1044 vs 1038 → +6ms）。⛔ 按旧口径，一旦残日落地，DoD 会渲染成「启动 P95 −496ms ↓」的假改善——
  ⚠️ 这也是**第 4 组把 cron 挪到 08:30 之前必须先落第 1 组**的原因：07:00(=23:00 UTC) 跑时残日还没落地，
  `MAX(DATE(...))` 侥幸取中完整日；挪到 08:30(=00:30 UTC) 残日已落地，旧口径当场翻车。

## 2. 性能段分域选版（B，仅 L1）

- [x] 2.1 只新增 `pick_versions_perf()`（`bin/lib/core/version.sh`）。⚠️ **没有新建 `pick_versions_crash()`**：既有的 `pick_newest()` 就是它，改名要动 6 处调用点却零行为变化，属于 CLAUDE.md「不改不坏的东西」。崩溃/放量选版**一字未动**
- [x] 2.2 `pick_versions_perf()` 候选降序、凑满即停、上限 `want+back`=4 个候选。⚠️ 「有没有值」的取法**改为一次 `SELECT DISTINCT app_display_version`**（`perf_versions_avail()`），不是逐候选一条查询：判据完全相同，查询数从最多 8 条降到 2 条
- [x] 2.3 判据只有「该版本在窗口内有没有行」。`MIN_SESSIONS` 未被触碰。⚠️ 回归用例的候选表**刻意不带会话数字段**——想掺会话量门槛的改法在测试里根本拿不到那个数
- [x] 2.4 4 个候选皆无、而性能表本身有行 → 打「⚠️ 数据未同步：…疑似导出退化」并停止回溯。⚠️ **用独立的 `PERF_VER_WARN` 而不是改 `data_state()`**：缺数三态的判据与顺序是硬约束，为一个新情况去动它风险远大于收益（回溯上限由 `pick_versions_perf` 自己保证，实测「前 4 个候选皆无」返回空）
- [x] 2.5 新增 `perf_only_tag()`，卡片表头 / 文档 markdown 表头 / 文档 XML 表头三处统一打「性能兜底」角标，卡片说明段补一句解释该列的崩溃/放量数字属于这个旧版本。

  ⚠️ **`ver_tag()` 是死代码，改它没有任何效果**：它唯一的调用点是 `build_table()`，而 `build_table()` 全仓无人调用（先于本 change 就已如此）。⛔ 按「不删无关死代码」只做记录，未删除。

  ⛔ 另修一处会被这个 change 变成谎话的地方：`ver_summary()` 原来把所有补充列一律写「+主力 1.5.3」，而性能兜底列的会话量可能很小，说成主力是睁眼说瞎话。改为逐列判断，现渲染「+性能兜底 1.5.3」
- [x] 2.6 验证（2026-08-27 16:48 dry-run，check-scripts 退出码 0、纯函数断言 70/70）：

  ```
  iOS     最新 2 版：1.5.5 1.5.4 · 主力：1.5.5 1.5.4 · 性能可得：1.5.4 1.5.3 · 卡片列：1.5.5 1.5.4 1.5.3
  Android 最新 2 版：1.5.5 1.5.4 · 主力：1.5.4 1.5.5 · 性能可得：1.5.4 1.5.3 · 卡片列：1.5.5 1.5.4 1.5.3
  ```

  两端性能可得均为 1.5.4 + 1.5.3（实测 1.5.5 在两端性能表窗口内都是 **0 行**）；1.5.5 在崩溃/放量段照常出现。
  卡片 `card.json` 实测 7 列（metric + c1…c6）6 行，⚠️ 列名仍是 `c1`…`c6`（`ios`/`android` 是 CardKit 平台变体键）。
  启动 P95 3d 一行：iOS `— 无数据 | 980ms | 738ms`，Android `— 无数据 | 3994ms | 2403ms`。

  ⛔ **渲染层有两个列集合定义点，第一版只改了一个**：`build_card_table()` 出 CardKit JSON、`md_table()` 出
  DRY RUN 预览与日报 markdown，两者各写一遍列循环。只改前者时预览显示 4 列、真发出去 6 列——
  **预览与实发不一致**是最难排查的一类，已在两处都留了「改一处必须改两处」的注释。

  ⛔ **列宽已实发验证，第一版真的被截了**（2026-08-27 17:04 发 `om_x100b67cb9…` 到开发机私聊）。
  ⚠️ **DRY RUN 与 markdown 预览都看不出列宽**——CardKit 表格总宽固定、7 列时按比例分配，
  只有真发一张才知道。验证手法：dry-run 产出的 `$STATE/publish/card.json` 直接用
  `lark-cli im +messages-send --user-id "$CRASH_REPORT_CHAT_ID" --msg-type interactive` 单发，
  不重跑链路、不写 state、不建文档（开发机 local.env 里 `CRASH_REPORT_CHAT_ID` 已是 `ou_` 私聊）。

  修法（17:18 复发 `om_x100b663441…` 确认通过）——**只短卡片，文档保持完整**：

  | | 之前 | 之后 |
  |---|---|---|
  | 表头角标 | `iOS 1.5.3 性能兜底` | `iOS 1.5.3·性能` |
  | 慢帧最差页 | `PaywallThemeCoursePopupViewController 54.2%（26 次）` | `PaywallTheme… 54.2%（26 次）` |

  新增纯函数 `screen_brief()`：先砍平台惯例后缀（`ViewController`/`Activity`/`Controller`/`Fragment`
  ——每个名字里都有，零区分度），再截到 12 字符；砍完够短就不加省略号。
  ⛔ **样本量括注没砍**：决策 D5「94% 在 3 次打开和 3000 次打开上是完全不同的结论」，砍了这行就不可判读。
  ⛔ **两端窗口标注（ANR 7d / 冻结 3d）也没砍**：同样是有记录的决策。
  ⚠️ 短角标「·性能」不再自解释，故卡片说明段那句完整解释**必须留着**。

  查询数：76（旧代码）→ 72（第 1 组后）→ **88**（本组后）。+16 = 每端多一个版本列的 7 条 × 2 端 + 2 条选版查询。

  纯函数断言：51（本 change 之前）→ 70（第 1 组后）→ **75**（本组后）。

## 3. 第 3 态细分（C，L1 + L2）

- [x] 3.1 已确认：`data_state()` 与 `state_text()` 的 diff **为空**（`git diff` 逐行核对），`table_max()` 顶上「刻意不带版本过滤」的注释与实现原样。细分逻辑放在**新函数** `state_text_perf()` 里，前两态原样委托回 `state_text()`，并有两条断言钉住文案一字不差
- [x] 3.2 `hist_lookup()`（值 + 日期）与 `hist_perf_last_day()`（该版本最后一次产出**任何**性能值的日期，判据用）
- [x] 3.3 分叉已落地，但**「沿用旧值」改为只给日期不给数值**——⛔ 五个性能格子没有一个能搬历史值：

  | 格子 | 格子口径 | 历史字段 | 历史口径 |
  |---|---|---|---|
  | 启动 P50 / P95 3d | 3 天滚动窗 | `start_p*_1d` | 单日 |
  | 慢帧最差页 3d | 最差页单页 | `slow_pct_1d` | 平台级聚合 |
  | 冻结率（最差页）3d | 最差页 | `frozen_pct_1d` | 平台级 |
  | 接口错误率 3d | 窗口 top10 端点 | `net_err_pct_1d` | 全量 |

  后两条 `daily-perf-1d.sql` 顶部自己写着「两套取数人群不同，不可直接混比」。把历史值放进标着「3d」的
  格子正是 `crash-metric-change` 核心事实 2 禁的事。⚠️ **判据仍然读历史**——「这版以前有没有产出过
  性能数据」是个布尔，与口径无关。最终文案：`⚠️ 本轮未取到（上次有值 2026-08-26）`。

  「预计 X 到位」的 X **由残日算出**，不是拍脑袋：`perf_version_scan()` 在同一条查询里多数一列
  「该版本在 > LCD 的残日有没有行」，有 ⇒ 明天 LCD 推进一天它就进窗口 ⇒ X = 今天+1。
  ⛔ 残日也没有就**不给日期**，回落既有第 3 态文案——空口承诺「明天就有」比不说更糟。
  ⚠️ 2026-08-27 实测这条守卫真的救了一次：iOS 1.5.5 在残日里**也是 0 行**（`in_tail=0`），
  照「新版滞后 1 天」的直觉写死就会印出一个不会兑现的「预计 08-28 到位」
- [x] 3.4 判据是 jq 的 `!= null`。⚠️ 端到端验证：注入一条「iOS 1.5.5 历史里只有 `frozen_pct_1d: "0.0"`、其余全 null」的合成记录，报告正确渲染成「⚠️ 本轮未取到」而不是「尚无数据」——`0` 被当作有值。字符串 `"0.0"` 与数字 `0` 各有一条断言
- [x] 3.5 长短两版都带日期：文档 `⚠️ 本轮未取到（上次有值 2026-08-26）`，卡片 `⚠️ 未取到 08-26`。⚠️ 卡片压缩时**砍措辞不砍日期**——日期才是「僵了多久」的唯一线索（同 `latest_event` 冻结在历史峰值那天的教训）
- [x] 3.6 L2 已接。⛔ **数据源是 `perf-history.jsonl` 不是 `weekly-metrics.jsonl`**——本条原文写错了文件名：`weekly-metrics.jsonl` 存的是**崩溃侧**周度基准（`crash`/`cfree`/`anr`/`nf`），里面没有任何性能字段；性能侧的历史在 `perf-history.jsonl`（`platform`/`version`/`day`/`p50`/`p95`）。L1 的 `metrics-history.jsonl` 同样不可混用（天级口径）。⚠️ L2 的呈现改为**表下注解**而不是填进格子——整行 6 个格子各重复一遍同一句话，正是 `CELL_BREVITY` 当初被发明出来解决的问题
- [x] 3.7 验证分两层：

  **函数级**（`bin/test/fn-regression.sh`，跑在生产 shell 设置下）12 条断言全过：全 null / 历史有值本轮 null /
  字符串 `"0.0"` / 数字 `0` / 残日有行 / 残日无行 / 前两态文案原样。
  ⚠️ 「两侧相等」这一种**结构上到不了第 3 态**：两侧都有值意味着 `prows > 0`，`data_state()` 判 `ok`，
  根本不会调用 `state_text_perf()`。故不构造该用例，改为在此说明原因。

  **端到端**（2026-08-27，注入合成历史行后 dry-run，验完已逐字节还原）：

  ```
  | 启动 P95 3d | ⚠️ 未取到 08-26 | ⚠️ 未取到 08-26 | 980ms | — 无数据 | 3994ms | 2403ms |
  ```
  iOS 1.5.6（历史有 p50/p95）与 iOS 1.5.5（历史**只有** `frozen_pct_1d:"0.0"`）都命中「本轮未取到」；
  Android 1.5.5（历史全 null）仍是「— 无数据」。⛔ 这一步不能省——函数级测试证明不了 `cell()` 里
  那个分支真的接上了新函数，而「接没接上」在两个分支都不可用时输出**与改动前完全相同**。

  ⚠️ 顺带记一笔：`bin/test/harness.sh` 原来只有 `contains`/`absent`/`rc` 三种断言，
  「文案一字未动」这类要求 contains 断不住（多打一句话照样通过），已补 `h_assert_eq`
- [x] 3.8 新增的注释与文案统一用「第 3 态细分」。⚠️ 既有的「缺数三态」措辞**保持不动**——它指的是原来那三态，改掉反而会让既有注释与 `data_state()` 对不上（3.1 要求那三态一字不动）

## 4. 跑批时间（D）

> ⏸ **本组不在本仓库**：改的是生产机 hermes cronjob。⚠️ design D9 要求与第 1 组同批切换——
> 第 1 组的验证已经证明这不只是「滞后持平」的问题：07:00(=23:00 UTC) 跑时残日还没落地，旧口径
> 侥幸取中完整日；**挪到 08:30(=00:30 UTC) 残日已落地，单独挪 cron 会直接制造假信号**。
> 故本组必须与第 1~3 组同一次部署，留到 deploy 时执行。

- [x] 4.1 2026-08-27 已改。生产机 Mac mini：`hermes cron edit 72f0850e6688 --schedule "30 8 * * *"`
  （`0 7 * * *` → `30 8 * * *`，next run 2026-08-28T08:30:00+08:00）。
  **回退**：`hermes cron edit 72f0850e6688 --schedule "0 7 * * *"`。
  ⚠️ job id：crash-daily = `72f0850e6688`，crash-weekly = `6d834f1f6da5`（`30 5 * * 1`，未改）。
  改后两条链路间隔 3 小时，满足「L2 与 L1 至少间隔 60 分钟」的既有约束。
  ⚠️ 非交互 ssh 里 `hermes cron list` 要先 `. ~/.local/state/crash-triage/path.env; export PATH`，
  否则 `update.sh` 会报「无 hermes cron 任务」——那是文档里记着的假故障
- [x] 4.2 同批：代码 `68afc40` 合入 main 并部署到生产（HEAD 核对一致、check-scripts 全绿）后，紧接着改 cron。

  ⚠️ **第 1 组的验证澄清了 D9 没说清的一点**——两者并非对称，顺序只能是「先代码后 cron」：

  | 组合 | 结果 |
  |---|---|
  | 新代码 + 旧 cron 07:00 | ✅ 安全。数据比目标态旧一天，但无假信号 |
  | 旧代码 + 新 cron 08:30 | ⛔ 危险。残日已落地，`MAX(DATE(...))` 取中 7 小时残日，DoD 直接出假改善 |
  | 新代码 + 新 cron | ✅ 目标态 |
- [ ] 4.3 ⏸ 待 2026-08-28 08:30 首轮跑批后核对。
  ⚠️ 判据：`health-daily.json` 的 `data_until` 应从 `2026-08-26 06:59 UTC` 前进到 `2026-08-27 06:59 UTC`
  （08:30 = 00:30 UTC，此时前一日分区已于约 23:54 UTC 落地）。
  连同 7.4 一起走 `/morning-verify`。

## 5. 口径断裂标记（design D8）

- [x] 5.1 `metrics-history.jsonl`（L1）与 **`perf-history.jsonl`**（L2）新行增 `window_mode`。

  ⛔ **不是 `weekly-metrics.jsonl`**——与 3.6 同一处笔误：那个文件存的是崩溃侧周度基准
  （`crash`/`cfree`/`anr`/`nf`），**崩溃口径本 change 一个字没改**，给它加 `window_mode` 纯属噪音。
  真正换了口径的是 L2 的性能历史 `perf-history.jsonl`（p50/p95，7 天窗 → 完整日闭区间）。

  ⚠️ 取值判据是「**这轮真的取到完整日窗口了吗**」而不是「代码是新版吗」：`PERF_LCD` 为空时
  性能段整段走缺数，仍写 `legacy`——标成 `complete_day` 会让明天的 WoW 以为两边可比。

  实测（沙箱 NO_DELIVER 整跑）新行：`{"day":"2026-08-27","window_mode":"complete_day",…}`，
  两个版本的 `perf_day` 都是 `2026-08-25`（= LCD）。
- [x] 5.2 实测旧行原样：`2026-08-20/21/22/23` 四行仍无 `window_mode` 字段，只有新写的 `2026-08-27` 有。读取侧一律把缺字段读作 `legacy`——⛔ 默认成 `complete_day` 等于把含残日的旧值和完整天值悄悄放在一起比
- [x] 5.3 两条链路都标：

  - **L1**：`dodwow_note()` 在 WoW 基准行是旧口径时追加「⚠️ 本行 WoW 跨口径：基准日 X 取的是旧口径
    （该版本自己的最新可用单日，可能含 7 小时残日），口径切换于 Y」。切换日 Y 由历史里第一条
    `complete_day` 记录算出，⚠️ 不写死日期。
  - **L2**：WoW 格子挂「⚠️跨口径」记号，完整解释在表下说一次——⛔ 每格重复一整句会把列宽撑爆
    （与卡片 6 列被截是同一条教训）。

  ⚠️ 「切换后 7 天内生效」**不需要计时器**：切满 7 天后 `LCD-7` 自然落进 `complete_day` 区间，
  标注自动消失。已有断言钉住这一点（基准同为新口径时不得误标）。

  ⚠️ 沙箱整跑里这条标注**没出现**，因为 `PERF_D7 = 2026-08-18` 那天历史里根本没有 1.5.6/1.5.5
  （版本还没发）——无基准不等于跨口径，不标是对的。故改由夹具覆盖：7 条断言分别钉住
  「缺字段读作 legacy」「有字段按字段读」「查不到基准日 → 空」「旧基准必标」「新基准不标」「无基准不标」。

## 6. 文档与 spec

- [x] 6.1 delta 写在 `specs/crash-perf-data-staleness-guard/spec.md`：ADDED 两条——「性能窗口只取完整日」（含「完整性判据不依赖分区元数据」「不得为此新增查询」两个 Scenario）与「窗口口径切换必须留痕且跨口径比较必须标注」。既有「容忍滞后」requirement 未改，两条并存
- [x] 6.2 ⛔ **不只是「补 Scenario」——既有 spec 里有一条与本 change 直接冲突，必须 MODIFY**：

  > #### Scenario: 新版在性能段常态无数据
  > - **AND** MUST NOT 回退到更旧的版本取数充数

  按字面，第 2 组的分域选版违反它。细读语义则不冲突——禁的是**把旧版数值填进新版的格子**（充数），
  而分域选版是**另起一列并显式标注**，新版格子里仍是它自己的缺数状态。已把这条改写成区分两者的措辞，
  ⛔ 「不得充数」保留为更强的约束，另起一列则显式放行。

  同一个 MODIFIED 里把第 3 态细分的三个 Scenario（尚无数据 / 本轮未取到 / 判据必须显式判 null）
  一并写入——它们本来就属于这条 requirement。⚠️ 既有三个 Scenario 的文案**逐字保留**（3.1 的要求）。

  另 ADDED「性能段按数据可得性独立选版」requirement。`openspec validate --all --strict` **28 passed 0 failed**
  （本 change 之前一直是 1 failed：`specs/` 目录不存在）
- [x] 6.3 两节都补了。「版本口径」节的分域说明**明确写成既有「刻意接受」段落的兑现而非推翻**——那段本来就写着「各表最新版本并不一致……各段各自解析必然错位」，既然错位是已认定的事实，与其让性能段摆一列空白，不如显示它自己表里真实的最新 2 版。「缺数三态」节补第 3 态细分表（含 ⛔ 显式判 null、⛔ 只给日期不给数值、⚠️ L2 历史源是 `perf-history.jsonl`）
- [x] 6.4 要求的两条是 **F33**（窗口容忍滞后 ≠ 窗口内数据完整）与 **F34**（缺「本轮未取到」态会把故障读成常态）。

  ⚠️ **另加了两条本次施工中真实踩到的**，不是凑数：

  - **F35 · 同一张表的列集合有两个定义点，改一个预览就骗过你了**——`build_card_table()` 出 CardKit JSON、
    `md_table()` 出 DRY RUN 预览，各写一遍列循环。只改前者的结果是**预览 4 列、实发 6 列**。
    比 F1 更阴：两份实现里的一份**就是验收工具本身**。
  - **F36 · 断言写在汇总函数之后：会跑、会打印 ✅、但不 gating**——12 条新断言被 `cat >>` 到
    `h_summary` 之后，照常打印绿勾却不进统计不影响退出码。

  超出 tasks 原定范围的还有两处（都与本 change 直接相关）：
  `docs/CLAUDE-测试盲区.md` 新增**盲区④「卡片列宽只有真发一张才验得出」**并补两条判据；
  根 `CLAUDE.md` 的硬约束清单补完整日窗口 / 分域选版 / 第 3 态细分 / `window_mode` / 列宽验证——
  ⚠️ 不补进 CLAUDE.md 的话，下一个会话看不到这些新硬约束

## 7. 验收

- [x] 7.1 冻结缓存下新旧并排（2026-08-27）：旧代码跑在 `git worktree` 的 `248f655`，新代码跑工作区，
  **共享同一个 `CRASH_REPORT_BQ_CACHE`**，各自一份沙箱 `XDG_STATE_HOME`，真实 STATE 全程未被碰。
  ⚠️ 未改动的 SQL（崩溃 / 会话 / 维度 / 版本解析）两边**文本相同 ⇒ 命中同一缓存条目 ⇒ 数据被冻住**，
  这正是让「1.5.6 中途出现」这类活数据漂移不污染 diff 的关键。

  日报文档 423 行 → 528 行，**269 行变化，逐行归因如下**：

  | 归因 | 行数 |
  |---|---|
  | A 性能明细表数值（窗口口径变了） | 143 |
  | A 性能行数值 | 30 |
  | A 完整日窗口标注 | 10 |
  | B 崩溃行追加一列 | 32 |
  | B 兜底列 / 角标 / 摘要 | 15 |
  | B 表格结构与明细表头（列数变了） | 19 |
  | B 新增的 1.5.3 明细小节 | 12 |
  | × 非代码差异（RUN_ID、跑批时刻、MCP 抓取成败） | 8 |

  **关键等价性断言**：新旧共有的版本列（1.5.6 / 1.5.5 / 1.5.4），崩溃与放量各行**逐字节一致**——
  脚本逐格比对 16 条（Crash-free / 崩溃次数 / 崩溃率 / 受影响安装 / ANR / ANR 率 / 非致命 / 会话数 × 两端），
  **16 条全过 0 条不一致**。⛔ 这条是 Non-goal「不改崩溃段与放量段」的机器化证据。

  **C 组差异 0 行**：1.5.6 / 1.5.5 在历史里没有性能值、在残日里也没有行，两个分支都不可用，
  回落到既有第 3 态文案——与旧代码输出相同。⚠️ 故 C 组的验证只能靠第 3 组那次注入合成历史的端到端跑与夹具，
  **本次 diff 证明不了 C 组**，如实记在这里。

  ⚠️ **冻结缓存冻不住 MCP**：旧跑的 `fetch-snapshot.sh` 抓取失败、新跑成功，于是新报告多出一行
  `🔴 Android 新增 6 个 issue（全版本口径）`。这不是代码差异（属 `docs/CLAUDE-测试盲区.md` 登记的 F18 类），
  但它说明**冻结缓存的「冻结面」只覆盖 bq**——等价性验收时 MCP 段的差异必须人工判读，不能一律当回归。
- [x] 7.2 退出码 0。⚠️ 施工过程中它拦下过三次真问题：全角紧邻变量（第 6 项）、顶层「先用后定」（第 7 项，`HIST_ARR` 用在定义前）、夹具里的同名函数（第 3 项）。三次都是真告警不是误报
- [x] 7.3 纯函数断言 51 → **75**；夹具断言 17 → **36**。覆盖：完整日判据（`last_complete_day` / `day_shift` / `win_days` 共 12 条，含跨月与不可解析输入）、
  兜底回溯上限（`pick_versions_perf` 7 条，含「前 4 个候选皆无就停手」与字典序反例 1.5.10 > 1.5.9）、
  第 3 态细分 12 条（含字符串 `"0.0"` 与数字 `0` 两种「0 是合法值」）、口径断裂标记 7 条、短页面名 5 条。

  ⛔ **新断言必须双向验证在 gating**：第一版把 12 条追加到 `h_summary` **之后**，它们照常打印 ✅ 却不进统计、
  不影响退出码（check-scripts 一直报「夹具 17 通过」）。已移到前面并实测：植入违规 → 退出码 1、`28 通过 1 失败`；
  还原 → 退出码 0。登记为失效模式 **F36**。
- [ ] 7.4 ⏸ **本地做不了，必须等真实跑批**：要连续两天的生产跑批结果才能观察 DoD 稳定性。
  与第 4 组（cron 07:00 → 08:30）同属部署后验证，一并留到上线后做。

  ⚠️ 观察判据写在这里以免届时忘了：两端同为完整日之后，环比**不应再出现 +30pp 级的构成性跳变**
  （提案 ② 记录的「慢帧 WoW +30.90pp」很可能整个是时段构成差异造出来的假象）。
  ⚠️ 切换后 7 天内 WoW 会带「跨口径」标注，那期间的差值本来就不该当真实变化读。
