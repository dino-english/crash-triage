## MODIFIED Requirements

### Requirement: 已有事实优先读本地

跑批时 MUST 先判定本地事实层是否已覆盖所需事件，命中则不重复抓取。

该判定 MUST 仅决定**是否发起事件明细抓取**，MUST NOT 同时决定是否更新观测字段——两者是不同的判断，观测字段的更新要求见「观测字段每轮无条件更新」。

判定所依据的线上事件计数 MAY 是某个时间窗口内的取值，因而**不保证单调递增**：窗口滑动会让老事件出窗，计数随之下降。实现 MUST NOT 假设该计数单调。

该判定 MUST 同时考虑**本地已存的事件条数**，MUST NOT 仅凭计数比较得出「已覆盖」。仅比较计数时，一条「计数为正、事件数组为空」的记录会在计数不再上涨后被永久判为命中，欠账无法收敛。

#### Scenario: 事实层已完整覆盖

- **WHEN** 某 issue 的本地事件数与线上事件数一致
- **THEN** 直接读取本地事实层
- **AND** 不发起该 issue 的事件详情抓取

#### Scenario: 线上有新增事件

- **WHEN** 某 issue 的线上事件数大于本地已存事件数
- **THEN** 只抓取新增部分并合并入本地事实层
- **AND** 既有事件记录不被改写

#### Scenario: 线上计数低于本地已存

- **WHEN** 某 issue 的线上事件数小于本地已存事件数（老事件滑出统计窗口所致）
- **THEN** 不发起事件详情抓取——计数下降意味着没有新事件
- **AND** 该 issue 的观测字段仍按「观测字段每轮无条件更新」被刷新
- **AND** 本地已有的累积事件记录不被删除或改写

#### Scenario: issue 已关闭且计数未变

- **WHEN** 某 issue 在线上已关闭且事件数与本地一致
- **THEN** 完全跳过该 issue 的抓取

#### Scenario: 计数为正但一条事件都没有

- **WHEN** 某 issue 的线上事件计数大于零，而本地事实层的事件数组为空
- **THEN** 该 issue MUST 被判为需要抓取，MUST NOT 被判为命中跳过
- **AND** 判定结果不因线上计数是否较上轮上涨而改变

## ADDED Requirements

### Requirement: 欠账补抓的节流与可见性

对「计数为正而事件数组为空」的欠账记录，系统 MUST 限制每轮补抓的 issue 数量，MUST NOT 在单轮内为全部欠账记录发起抓取。

事实层断言 MUST 输出当前欠账 issue 的数量，使补齐进度可被逐轮观察。

理由：欠账是历史积累的存量，一次性放开会在单轮内产生数十次事件明细抓取；而不给出存量数字，补齐是否在收敛就无从判断。

#### Scenario: 欠账多于单轮上限

- **WHEN** 某轮观测到的欠账 issue 数超过单轮补抓上限
- **THEN** 本轮只为上限数量的 issue 发起抓取
- **AND** 其余欠账 issue 在后续轮次继续补抓

#### Scenario: 补齐进度可见

- **WHEN** 事实层断言执行
- **THEN** 输出中包含当前欠账 issue 数量
- **AND** 该输出不改变断言的退出码
