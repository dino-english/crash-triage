## 1. 基线取证

- [ ] 1.1 记录当前欠账清单：逐个 issue 的 `events_count_last_seen` 与 `(.events|length)`，
      标出「计数 > 0 且数组为空」的条目
      → 验：条目数与 2026-09-09 生产实测（本轮 6 个 issue 中 4 个欠账）可对照
- [ ] 1.2 估算补抓成本：欠账数 × 每 issue 一次 `list_events`
      → 验：给出数字，并据此定节流默认值（提案暂定 3/轮）

## 2. 核心层：判定看得见内容

- [ ] 2.1 `cache_verdict` 增第 5 个入参「已存事件条数」；「计数 > 0 且已存 == 0」→ `append`
      → 验：`bin/test/run.sh` 新增用例——欠账 → append · 已有事件且计数未涨 → skip ·
      计数下降 → skip（⛔ 原语义不得回归）
- [ ] 2.2 ⚠️ 改签名会让**所有调用点**报错，正好当清单用：`grep -rn cache_verdict bin/`
      逐个补参，⛔ 不得用默认值兜底——默认值会让漏改的调用点静默走旧行为
      → 验：`grep -c cache_verdict` 的调用点数 == 已改数
- [ ] 2.3 ⛔ 判据只取「已存 == 0」这一种保守形态，**不做「短缺比例」**——
      `events` 是累积数组、计数是滚动窗口，两者不可比（见 F-5）
      → 验：用例里显式钉住「已存 5 · 计数 29 → skip」，证明没有偷偷加比例判据

## 3. 外壳层：节流与可见性

- [ ] 3.1 两条路径的调用处传入 `(.events // []) | length`
- [ ] 3.2 单轮补抓上限（默认 3，`CRASH_REPORT_BACKFILL_LIMIT` 可配）
      → 验：夹具造 5 个欠账，确认本轮只放行 3 个
- [ ] 3.3 `assert-fact-cache.sh` 的事件数对照行增「欠账 N 个」
      → 验：当前生产数据下该数字非零且与 1.1 的清单一致
- [ ] 3.4 `bash bin/check-scripts.sh` 通过

## 4. 验证

- [ ] 4.1 用 `bin/test/fn-fetch-snapshot-shell.sh` 的密封夹具跑欠账场景（零模型调用）
      → 验：欠账 issue 被判 append 而非 skip；⛔ 双向测试——把判据改回只看计数，夹具须变红
- [ ] 4.2 冻结缓存等价性：⛔ **本 change 不是零行为重构**，判定结果**刻意改变**。
      故等价性只验「非欠账记录的行为未变」
      → 验：对 `events` 非空的记录，新旧判定结果逐条相同
- [ ] 4.3 生产部署后连续 3 轮观察欠账数是否单调下降
      → 验：三轮的「欠账 N 个」构成下降序列。⛔ 不下降就是没在收敛，
      **不得以「代码路径对」结案**（pending-2026-09-06-verify 的教训）
