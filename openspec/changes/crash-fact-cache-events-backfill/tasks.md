## 1. 基线取证

- [x] 1.1 记录当前欠账清单：逐个 issue 的 `events_count_last_seen` 与 `(.events|length)`，
      标出「计数 > 0 且数组为空」的条目
      → 验：条目数与 2026-09-09 生产实测（本轮 6 个 issue 中 4 个欠账）可对照
      **实测**：24 个文件中 **18 个欠账**（计数合计 59 · 已存合计 6 · 有事件的 6 个）
- [x] 1.2 估算补抓成本：欠账数 × 每 issue 一次 `list_events`
      → 验：给出数字，并据此定节流默认值（提案暂定 3/轮）
      **18 次 `list_events`，按 3/轮约 6 轮补完**；且只有出现在当轮快照里的 issue 才会被处理，
      实际更慢。节流定 3 不变。

## 2. 核心层：判定看得见内容

- [x] 2.1 `cache_verdict` 增第 5 个入参「已存事件条数」；「计数 > 0 且已存 == 0」→ `append`
      → 验：`bin/test/run.sh` 新增用例——欠账 → append · 已有事件且计数未涨 → skip ·
      计数下降 → skip（⛔ 原语义不得回归）
- [x] 2.2 ⚠️ 改签名会让**所有调用点**报错，正好当清单用：`grep -rn cache_verdict bin/`
      逐个补参，⛔ 不得用默认值兜底——默认值会让漏改的调用点静默走旧行为
      → 验：`grep -c cache_verdict` 的调用点数 == 已改数
- [x] 2.3 ⛔ 判据只取「已存 == 0」这一种保守形态，**不做「短缺比例」**——
      `events` 是累积数组、计数是滚动窗口，两者不可比（见 F-5）
      → 验：用例里显式钉住「已存 5 · 计数 29 → skip」，证明没有偷偷加比例判据

## 3. 外壳层：节流与可见性

- [x] 3.1 bq 路径调用处传入 `(.events // []) | length`（⚠️ 只有这一个 `cache_verdict` 调用点）
- [x] 3.2 模型路径：shell 算欠账清单、取前 N（默认 3，`CRASH_REPORT_BACKFILL_LIMIT`）
      **显式写进 prompt**（design D1——⛔ 不让模型自己判断数组空不空，它已被证明会跳过读文件）
      → 验：密封夹具让桩把 argv 落盘，断言 prompt 里恰好出现 3 个 id 且按序
- [x] 3.3 `assert-fact-cache.sh` 的事件数对照行增「欠账 N 个」
      → 验：当前生产数据下该数字非零且与 1.1 的清单一致
- [x] 3.4 `bash bin/check-scripts.sh` 通过

## 4. 验证

- [x] 4.1 用 `bin/test/fn-fetch-snapshot-shell.sh` 的密封夹具跑欠账场景（零模型调用）
      → 验：欠账 issue 被判 append 而非 skip；⛔ 双向测试——把判据改回只看计数，夹具须变红
- [ ] 4.2 冻结缓存等价性：⛔ **本 change 不是零行为重构**，判定结果**刻意改变**。
      故等价性只验「非欠账记录的行为未变」
      → 验：对 `events` 非空的记录，新旧判定结果逐条相同
- [ ] 4.3 生产部署后连续 3 轮观察欠账数是否单调下降
      → 验：三轮的「欠账 N 个」构成下降序列。⛔ 不下降就是没在收敛，
      **不得以「代码路径对」结案**（pending-2026-09-06-verify 的教训）

## 实施记录（2026-09-09）

- `cache_verdict` 增第 5 参，判定顺序：强制 → 文件不存在 → **欠账** → 计数上涨 → 跳过。
  ⚠️ 缺参按 `-1`（未知）退回旧行为——⛔ 不默认 0，那会让漏改的调用点每轮重抓。
- 生产调用点只有 `fetch-snapshot-bq.sh` 一个；模型路径的判定在 prompt 里，
  故补抓清单由 shell 算好后**显式注入 prompt**（design D1）。
- 双向测试：去掉节流 → 夹具 2 条红；判定退回只看计数 → 纯函数断言 1 条红。
- `check-scripts.sh` 十项通过（纯函数 82 条 · 壳层夹具 22 条）。
