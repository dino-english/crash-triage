## 1. SQL

- [x] 1.1 新增 `crash-issue-drilldown.sql`：六维度 × top1 + 取值基数 + issue 总量
- [x] 1.2 自选 top N（`{{TOP_N}}`），按受影响安装降序
- [x] 1.3 title + subtitle 都输出（D3）
- [x] 1.4 前后台复用 `{{FG_NORM}}`，⛔ 不复制 CASE 表达式（F1）
- [x] 1.5 行序确定性：issue 按安装降序，维度按可行动性排（页面最前）

## 2. 取数与渲染

- [x] 2.1 `dd_fetch()` 定义在**调用点之前**（F23 同类：顺序错则 `command not found` 被 `|| true` 吞）
- [x] 2.2 `dd_block()` 渲染，机型走三分支
- [x] 2.3 ⛔ 经 `csv2tsv` 解析（机型含逗号、标题是自由文本）
- [x] 2.4 区分「无 issue」与「取数失败」，后者渲染 `⚠️ 下钻取数失败，本段缺失——不代表没有 issue`
- [x] 2.5 常量 `DD_TOP_N` / `DD_MODEL_CONC_PCT` 定义在取数区

## 3. 验收

- [x] 3.1 函数级：三分支全验（分散 / 单设备 / 真集中，末者用构造数据）
- [x] 3.2 列序 bug 修复后复测正确
- [x] 3.3 L2 整跑 rc=0，断言**下钻段必须有实际条目**（6 个页面行）
- [x] 3.4 段落编号顺延核对（一…六）

## 4. 未决

- [ ] 4.1 ⚠️ 机型集中阈值 60% 未回测——建议连续 4 周打印命中数与被判「分散」的清单再定
