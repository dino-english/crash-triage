## 1. 实现

- [x] 1.1 `SEEN_WAS_ESTABLISHED` 在 `render-ledger.sh` **之前**判定（之后基准会被覆盖成本轮值）
- [x] 1.2 复发数直接数已渲染的现状表，⛔ 不重算三态（F4：同一个值多来源必然漂移）
- [x] 1.3 三态文案：`本轮建立基准` / `0 / N（无 issue 回归）` / `N / M（…）`
- [x] 1.4 挂进「一、本周变化」段并附口径标注

## 2. 验收

- [x] 2.1 函数级 4 用例（基准未建立 / 无回归 / 有回归 / 表文件不存在）
- [x] 2.2 L2 整跑 rc=0，产物 `**本周复发**：0 / 14（无 issue 回归）`
- [x] 2.3 ⚠️ 过程中踩 F21：`grep -c … || echo 0` 零匹配时得到 `"0\n0"`，整数比较报错。⚠️ **只在零匹配路径炸**——而生产上绝大多数周恰恰零回归

## 3. 已知边界（未解决）

- [ ] 3.1 ⚠️ 台账取数带 `LIMIT 20`：issue 总数逼近 20 时，掉出榜单再回来会被误计为回归。实测 Android FATAL 7d 唯一 issue 数 **13 < 20**，暂未咬合——触发时复发数会虚高
