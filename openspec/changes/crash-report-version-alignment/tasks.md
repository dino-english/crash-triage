## 0. 前置

- [ ] 0.1 ⚠️ 走 `crash-metric-change` skill 的校验流程，确认口径红线（本 change 改的是版本集合，不改任何指标算法）
- [x] 0.2 判据形态定案：A（7 天 top2 ∪ 当日 top1，上限 3）——2026-09-01 确认

## 1. 版本解析

- [ ] 1.1 `crash-weekly.sh:432` 的 `top2_versions()` 之外增「当日 top1」解析，复用 `latest-versions.sql`，`DAYS=1`
- [ ] 1.2 ⛔ 不加任何会话量门槛（design D4）——排序取 top1，不筛选
- [ ] 1.3 并集去重，显式断言结果 ≤3 个版本（design D2）
- [ ] 1.4 每个版本带入选理由标签（`窗口主力` / `当日主力`），随版本集合一起向下游传递

## 2. 渲染

- [ ] 2.1 「二、主力版本」表逐列标注入选理由；⛔ 不得一律写「主力」
- [ ] 2.2 摘要行标明判定对象所属版本与理由
- [ ] 2.3 「三、影响面分布」「四、TOP3 下钻」跟随新集合；口径注解补「当日主力那一版 7 天样本可能很小」
- [ ] 2.4 口径段（六）增一句：主力版本集合的双判据说明
- [ ] 2.5 ⛔ 全角括号与 `·` 先条件赋值再拼接（`check-scripts.sh` 第二项）
- [ ] 2.6 卡片形态确认：两集合重合时列数不变；⚠️ 补列后卡片列宽**只有真发一张才验得出**，单发 `$STATE/publish/card.json` 到开发机 `ou_` 私聊

## 3. 静态检查与测试

- [ ] 3.1 `bash bin/check-scripts.sh` 九项通过
- [ ] 3.2 函数级测试跑在 `bin/test/harness.sh` 生产 shell 设置下
- [ ] 3.3 夹具测试：两集合重合 → 2 列；分歧 → 3 列且理由标签正确；当日 top1 只有 1 个会话 → 仍补入且带 ⚠️

## 4. 等价性与跑批验收

- [ ] 4.1 ⚠️ 先备份 `$STATE/last-snapshot.json` 与 `$STATE/issue-seen.json`
- [ ] 4.2 稳态等价性：重合周的冻结缓存重放，三层产物 `diff -r` 为空（⛔ `CRASH_REPORT_BQ_CACHE` 生产禁用）
- [ ] 4.3 放量期形态：08-31 那轮的冻结缓存重放，断言集合含 1.5.6 且标为「当日主力」
- [ ] 4.4 数值盲比对：补列版本的会话/设备/崩溃数与 BigQuery 直查一致
- [ ] 4.5 `CRASH_REPORT_NO_DELIVER=1` 整跑 rc=0；⚠️ 整跑 5 分钟以上，别设短超时
- [ ] 4.6 恢复备份的两个基准文件并逐字节校验

## 5. 文档

- [ ] 5.1 `docs/CLAUDE-架构与数据口径.md` 版本口径段更新
- [ ] 5.2 CLAUDE.md 常驻硬约束更新周报版本口径那一行
- [ ] 5.3 `docs/CLAUDE-失效模式登记.md` 增一条：累计量作为版本判据在交替期必然滞后
- [ ] 5.4 归档本 change
