- [x] 1.1 `ca3cd5b` 断言两种非零退出分流（逐 issue 行 vs 自身护栏），原因照抄被调用方
- [x] 1.2 回归用例直接用 09-05 生产的空快照形态（不得出现「缓存未刷新」、不得谎称 issue 个数）
- [x] 2.1 `7db18ab` 空快照用 BigQuery 交叉印证；两侧都 0 ⇒ 只记日志不告警
- [x] 2.2 `CUR_IDS` 上移至 MCP 段之前复用（⛔ 只移位置不改表达式，不复制第二份定义）
- [x] 3.1 `976b685` Android 修复提交改为渲染 fix_commit；⛔ null 仍不渲染「🔴 未修」
- [x] 3.2 FIXED_PENDING 纳入 Android（非 null 在两端都无歧义）
- [x] 3.3 前提在**生产机业务仓**复核（本机 clone 落后且正则写错，一度得出反向结论）
- [x] 4.1 `1789add` alert_ver 两个回退条件；SAMPLE_SESSION_MIN 30 → 213（附推导）
- [x] 4.2 回退原因逐端照抄，跑批日志只列真的回退了的那一端
- [x] 4.3 用 09-04 真实产物端到端复核：回退至 1.5.6，ANR 0.77% 与慢帧 95.7% 双双触发
- [x] 4.4 09-05 数据在新旧门槛下判定对象一致，确认无回归
- [x] 5.1 `1789add` check-scripts 第 4 项排除 bin/test/；其余各项仍递归覆盖
- [x] 5.2 双向测试：跨生产文件 / 生产 vs lib 仍检出；夹具间重名与打桩不再报
- [x] 5.3 ⚠️ 事后搜索发现同一条过期结论在 **L2 还活着**（crash-weekly.sh:924）——
      L1 是「没跑扫描器所以没数据」，L2 是**跑了、拿到 4 条 Android 命中、在渲染处扔掉**。
      「代码已修待验」正是该段注释自己写的「最容易被遗忘的状态，必须顶到卡片上」。已修，13 条断言。
- [x] 5.4 新增 check-scripts 第 10 项「过期论断 lint」：已登记的、已被订正的结论不得复活。
      ⚠️ 能力边界写进注释与文档：只认登记在册的，认不出没登记的。
      验证：把 crash-weekly.sh 恢复成修复前的版本，lint 精确命中我漏掉的那一行。
- [x] 5.5 「九项」表述在 6 处对齐为「十项」；⚠️ `docs/CLAUDE-部署与运维.md` 那份清单本身
      早已与脚本不符（列 7 条却写九项，条目也对不上），一并按脚本头部重写。
      ⛔ openspec/changes/archive/** 里的 7 处不改——那是历史记录，写下时是准确的。
- [x] 6.1 部署后首个跑批日核验（2026-09-06 L1 / 2026-09-07 L2）——⚠️ 上一轮正是在这一步暴露出假告警。
      本轮 morning-verify 四项全过；**但 5.3 那条修复的验收判据不成立**：
      那 4 条 Android 修复提交全是 08-19，而 `FIX_SCAN_DAYS` 默认 14 天（生产 local.env 未覆盖，已核），
      写下 5.3 时窗口起点就已经是 08-22。生产本轮 fixmap 实测 `n:2`，全是 iOS。
      ⚠️ `7db18ab` 的空快照判据两轮都没走到，至今未验。
- [x] 7.1 5.3 只拆了平台过滤，**数据源没换**：卡片走 `DIFF.$k.fixed_pending`（源自当周快照），
      台账走 `fixmap.mapped`。09-07 同一轮：台账 2 条、卡片 0 条。改 `_fix_rows()` 直读 fixmap。
- [x] 7.2 状态改用 fixmap 自己的 `status`，不再写死「已修待验」——旧写法会把「修了仍在」说反。
- [x] 7.3 移除 DIFF 里已成孤儿的 `fixed_pending` 字段，并在原地留防复活说明。
- [x] 7.4 新增夹具 `bin/test/fn-fixed-pending.sh`（14 条断言）。⚠️ 5.3 当初没有任何夹具覆盖渲染处，
      这正是它「测试全绿但输出恒为 0」的原因。
- [x] 7.5 **双向测试**：对 git HEAD 逐字搬来的旧实现跑同一套断言 → 5 条红，且红在
      「①快照为空漏报」「③把修了仍在写成已修待验」上；⚠️ 第一次做反向测试时旧实现副本引号被转义搞坏，
      红的是「函数加载失败」而非逻辑差异——**判据答非所问**，已重做。
- [x] 7.6 用 09-07 生产原始 fixmap/snapshot 回放：旧实现 0 条（与实发卡片一致），新实现出 2 条。
- [ ] 7.7 部署后于 2026-09-14 L2 核验卡片上确有「🛠️ 代码已修待验」两条 ⛔ 未核验前不得归档
- [x] 8.1 L1 摘要行补回归分列：基准落 `daily-snapshot.json` 的 `mcp_seen` 字段（MCP OPEN 全版本 id 空间）。
      ⛔ 不借用 `issue_seen`（BigQuery 最新 2 版口径，借来即跨口径给数）；
      ⛔ 不新建第三个文件——复用既有快照的保留期与清理，不重开三个已知的坑。
- [x] 8.2 基准未建立时不分列，只报合计并说明（同 1789add「判定对象要看答不答得了」）。
- [x] 8.3 订正过期论断「ios_ids/android_ids 长期为空 / L1 新增判定已经死了」，并登记进第 10 项 lint。
      ⚠️ 双向测试时 lint 抓到了我自己新写的注释（论断与「已订正」标记不在同一行），已改。
- [x] 8.4 新增夹具 `bin/test/fn-mcp-lifecycle.sh`（9 条断言，场景取自 09-07 生产实况）。
- [x] 8.5 双向测试：对 git HEAD 逐字搬来的两态实现跑同一套断言 → 5 条红，
      ①「1 新增 + 1 回归」被算成「新增 2」。⚠️ 顺带发现旧实现在 MCP 抓取失败时返回空串而非 0。
- [x] 8.6 **全生产数据回放**：用 runs/ 里 14 天的 MCP 快照回填基准、以 09-06 的 OPEN 集合作上一轮，
      旧实现出「新增 1」、新实现出「回归 1」，那一条正是 `63db031f`——与当日正文表一致。
- [x] 8.7 `crash-weekly.sh` 脚注「基准规模小（实测 14 项）」改为现算（08-31/09-07 两周写死同一个数，
      而真实规模是 10 / 16）。⚠️ 有记录的口径标注不能是字面量。
- [ ] 8.8 部署后于 2026-09-08 08:30 L1 核验：⚠️ 首轮 `mcp_seen` 为空，预期出现
      「本轮建立基准，未区分新增与回归」文案；**09-09 起**才该看到真正的「🔁 回归」分列
